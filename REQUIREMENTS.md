# ExcelMCP 需求池

> 详细需求内容，需求状态变化时更新。
> 当前状态概览见 [docs/NOW.md](docs/NOW.md)，路线图见 [docs/ROADMAP.md](docs/ROADMAP)，已归档见 [ARCHIVED.md](ARCHIVED.md)。

## 活跃需求

### REQ-035 [P1] MCP真实验证发现问题修复
**来源**：第185轮MCP真实验证发现
**问题描述**：MCP真实验证发现7项核心功能存在严重问题，影响游戏开发场景的可用性
**具体问题**：
1. **get_range格式错误**：范围表达式要求必须包含工作表名和感叹号（格式：'Sheet1!A1:C10'），但API文档未明确说明
2. **SQL解析器编码问题**：query系列功能无法正确解析中文表名，出现乱码`\u001b[4m[\u001b[0m`，导致SQL语法错误
3. **batch_insert_rows验证错误**：数据验证逻辑过于严格，拒绝有效的行数据列表，提示"数据不能为空"
4. **delete_rows类型错误**：行删除操作中存在类型比较错误，'<'操作符不支持str和int类型比较
5. **query WHERE功能异常**：无法执行基本的条件查询，表名解析失败
6. **query JOIN功能异常**：无法执行跨表关联查询，同样受SQL解析器问题影响
7. **query GROUP BY/子查询功能异常**：所有SQL相关功能都受到解析器问题影响
**影响范围**：所有SQL查询功能、范围查询功能、数据修改功能基本不可用
**优先级**：P1 - 核心功能失效，必须立即修复
**解决方案**：
1. 修复SQL解析器对中文表名的支持
2. 修正get_range的参数格式验证和文档说明
3. 优化数据验证逻辑，平衡严格性和可用性
4. 修复类型比较错误
5. 添加更多测试用例验证修复效果
**验收标准**：
- 所有12项MCP核心功能通过测试验证
- 支持中文表名的SQL查询
- 数据验证逻辑正确且用户友好
- 提供清晰的错误提示和使用文档

## 已完成需求

### REQ-034 [P1] 边界值和性能优化 + 代码整洁度清理 ✅
**完成轮次**：第171轮（2026-03-28）
**来源1**：REQ-033极限探索发现（边界值和性能优化）
**来源2**：CEO指示 + 项目健康度分析（代码整洁度）
**问题描述**：
1. **边界值优化**：冷却时间0.1秒精度损失、大数值超出范围、NULL值处理异常
2. **性能优化**：复杂JOIN查询性能下降、子查询嵌套过深、GROUP BY内存占用高
3. **代码整洁度**：根目录16个临时脚本（2301行）需要清理，测试文件重复和功能重叠
**解决方案**：
- 边界值：23个测试用例覆盖浮点精度、大数值、NULL、极小值
- 性能：JOIN缓存机制+索引优化，3个性能测试通过
- 整洁度：删除6283行冗余代码，清理根目录临时文件
**验收标准**：
- ✅ 边界值测试23个通过，性能提升显著
- ✅ 根目录无临时脚本
- ✅ 全量测试1164+通过，v1.6.30发布PyPI
