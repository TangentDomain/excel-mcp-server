# ExcelMCP 进化评分卡

> 评分标准可以动态调整，调整时记录原因。

## 评分维度（总分100）

| 维度 | 权重 | 评估方式 |
|------|------|----------|
| 测试覆盖 | 25分 | pytest通过率 + 测试质量 |
| 游戏场景适配 | 25分 | 游戏配置表操作是否完整流畅（双行表头/批量操作/版本对比/SQL查询） |
| AI可用性 | 20分 | Claude调用成功率、参数理解准确度、错误自修正能力 |
| 文档质量 | 15分 | README/docstring完整性、示例丰富度、AI能否不看文档就上手 |
| 代码质量 | 10分 | 无冗余、结构清晰、错误处理完善 |
| 性能 | 5分 | 大表（1000+行）查询和批量操作响应速度 |

## 评分记录

### 第4轮 (2026-03-25 20:10 UTC) — 82/100

| 维度 | 得分 | 说明 |
|------|------|------|
| 测试覆盖 | 22/25 | 95个SQL测试全通过，新增游戏配置表fixture，ORDER BY别名场景覆盖 |
| 游戏场景适配 | 23/25 | ✅双行表头自动识别+中文列名查询 ✅ORDER BY引用别名(数值平衡DPM) ✅COUNT(*)无别名→count_star ✅WHERE/GROUP BY/HAVING/ORDER BY/LIMIT完整 ✅中英文混合查询 ✅SELECT*去重 ❌JOIN不支持 ❌CASE WHEN不支持 |
| AI可用性 | 18/20 | ✅Claude Code评估8.5/10 ✅中文列名让策划零学习成本 ✅错误提示清晰(不支持的语法有替代方案) ❌缺少DESCRIBE命令快速查看表结构 |
| 文档质量 | 13/15 | ✅README新增SQL查询参考(支持的/不支持的语法) ✅双行表头说明更新(自动识别) ✅验证命令修正(38→39) ❌SQL功能说明可以更详细(数学表达式/双行表头行为) |
| 代码质量 | 6/10 | ✅_resolve_order_column统一处理别名+原始列 ✅_generate_aggregate_alias复用 ✅cn_to_en_map中文映射逻辑清晰 ⚠️中文别名列在DataFrame中临时创建再清理，不够优雅 |
| 性能 | 0/5 | ⚠️未评估（2000行表各项查询200-250ms，主要瓶颈在openpyxl文件加载，非SQL引擎本身） |

**总评：82/100**（上轮72，+10）

**最低维度：性能（未评估）→ 代码质量**

**本轮改进：**
1. README补全SQL查询参考（支持的/不支持的语法+双行表头说明）
2. COUNT(*)无别名生成有意义列名（count_star, avg_damage）
3. 双行表头支持中文列名查询（策划用中文WHERE/GROUP BY/ORDER BY）
4. ORDER BY同时支持引用SELECT别名和非SELECT列
5. SELECT*结果去重（中文别名列不重复返回）
6. 新增游戏配置表测试fixture（技能表+装备表）

**下轮方向：**
1. P0：性能优化 — 考虑openpyxl → openpyxl read_only流式加载，或缓存已加载的DataFrame
2. P1：DESCRIBE命令 — 快速查看表结构（列名+类型+描述），减少SELECT *的需求
3. P1：中文别名列注册方式优化（不在DataFrame中临时创建，改为SQL解析阶段替换）
4. P2：错误提示增加SQL示例（如"试试: SELECT 技能类型, AVG(伤害) FROM 技能配置 GROUP BY 技能类型"）
5. P2：添加DISTINCT支持测试
