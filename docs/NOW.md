# NOW — 当前状态

> 子代理每轮必读必写。CEO可改，子代理必改。≤30行，强制精简。

## 状态
版本：v1.1.0 | 工具：44 | 测试：1047 | 评分：100/100

## 正在做
- [ ] REQ-026 文档与门面优化（下一步）

## 待做
1. REQ-026 文档与门面优化（README 30秒上手教程、竞品对比、SQL示例）
2. REQ-015 写入性能优化
3. REQ-012 多客户端兼容性验证

## 上一轮完成
- 第84轮：REQ-028 FROM子查询支持
  - FROM (SELECT ...) AS alias 语法完整实现
  - 支持外层WHERE/JOIN/聚合/ORDER BY+LIMIT
  - 拒绝嵌套FROM子查询（防止无限递归）
  - +12个新测试，1047 tests passing

## 阻塞项
- 无
