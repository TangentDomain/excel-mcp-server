# ExcelMCP 迭代路线图

> 记录回归测试中发现的问题，按优先级排列，供后续迭代修复。

---

## 📊 回归测试基线（2026-04-13）

**CI 状态**：✅ Run `24350175280` SUCCESS（3m2s）
**代码质量**：ruff format + lint 全通过（52 files reformatted, 694 auto-fixes）

| 测试套件 | 通过率 | 状态 |
|----------|--------|------|
| Round 5 | 34/34 (100%) | ✅ |
| Round 6 | 33/34 (97%) | ⚠️ |
| Round 7 | 32/32 (100%) | ✅ |
| Round 8 | 部分失败 | ❌ |
| Round 10 | 部分失败 | ❌ |
| MCP Validation | 未完成 | ⏳ |

---

## 🔴 待修复问题清单

### P0 - 数据回读一致性

| # | 问题 | 测试套件 | 触发场景 | 备注 |
|---|------|----------|----------|------|
| 1 | **INSERT 后 SELECT 回读验证失败** | Round 8 | 插入数据后立即查询验证 | 可能是写入缓存/刷新时序问题 |
| 2 | **超长文本回读完整性** | Round 10 | INSERT 超长文本后 LENGTH() 验证 | 截断或编码问题 |
| 3 | **中文特殊符号回读异常** | Round 10 | 中文 + 特殊符号组合值 | 编码或转义问题 |
| 4 | **COALESCE 结果回读不一致** | Round 10 | COALESCE 空值处理 | 聚合函数结果类型映射 |

### P1 - SQL 边缘 Case

| # | 问题 | 测试套件 | 触发场景 | 备注 |
|---|------|----------|----------|------|
| 5 | **INTERSECT 合并类型冲突** | Round 8 | INTERSECT on object + float64 列 | pandas merge 类型推断问题 |
| 6 | **超长文本 WHERE 筛选** | Round 6 / Round 8 | WHERE 子句匹配超长文本 | LIKE 查询或字符串比较边界 |
| 7 | **特殊字符值处理** | Round 8 | 引号、符号、HTML 标签 | SQL 注入防护转义过度？ |

### P2 - 测试完善

| # | 问题 | 测试套件 | 触发场景 | 备注 |
|---|------|----------|----------|------|
| 8 | **MCP Validation 未完成** | mcp_validation_test | 执行中断 | 需要完整跑一遍 |

---

## 🛠️ 修复建议

### P0 - 数据回读
- 检查 `excel_writer.py` 写入后是否需要显式 `flush()` / `save()` 
- 确认 `excel_reader.py` 读取是否命中缓存而非最新文件
- 验证 UTF-8 编码链路（写入 → 存储 → 读取）完整性

### P1 - SQL 边缘 Case
- INTERSECT：检查 `advanced_sql_query.py` 中 merge 操作的 dtype 处理
- 超长文本：确认 pandas 字符串比较是否有长度限制
- 特殊字符：审查 SQL 参数化转义逻辑

---

## 📝 迭代记录

### v1.9.9（待规划）
- [ ] 修复 P0 数据回读一致性问题（#1 ~ #4）
- [ ] 修复 INTERSECT 类型冲突（#5）
- [ ] 完善超长文本 / 特殊字符处理（#6 ~ #7）
- [ ] 补全 MCP Validation 测试（#8）

---

*最后更新：2026-04-15 15:04 北京时间*
