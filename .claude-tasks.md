# ExcelMCP 任务拆解分析

## 任务概览

根据 REQUIREMENTS.md 分析，以下为需要处理的任务：

| REQ | 标题 | 优先级 | MD状态 | 实际状态 | Git提交 |
|-----|------|--------|--------|----------|---------|
| REQ-054 | 嵌套子查询只返回1行 | P1 | IN-PROGRESS | ✅ 已完成 | 4dbf6c1 |
| REQ-059 | HAVING不过滤TOTAL行 | P1 | IN-PROGRESS | ✅ 已完成 | 297043f |
| REQ-055 | 支持EXCEPT/INTERSECT | P2 | OPEN | ✅ 已完成 | 75b87be |
| REQ-056 | 支持CTE | P3 | OPEN | ⚠️ 已实现需验证 | - |
| REQ-057 | 支持窗口函数 | P3 | OPEN | ⚠️ 已实现需验证 | - |
| REQ-060 | 子查询IN返回全量数据 | P2 | OPEN | ❌ 需修复 | - |

---

## 子任务列表

### 子任务 1: 更新REQ-054状态为DONE
- 文件: REQUIREMENTS.md
- 改动: 将REQ-054的status从"IN-PROGRESS"改为"DONE"，添加完成说明
- 预估: 3分钟
- 依赖: 无

### 子任务 2: 更新REQ-059状态为DONE
- 文件: REQUIREMENTS.md
- 改动: 将REQ-059的status从"IN-PROGRESS"改为"DONE"，添加完成说明
- 预估: 3分钟
- 依赖: 无

### 子任务 3: 更新REQ-055状态为DONE
- 文件: REQUIREMENTS.md
- 改动: 将REQ-055的status从"OPEN"改为"DONE"，添加完成说明
- 预估: 3分钟
- 依赖: 无

### 子任务 4: 验证REQ-056 CTE功能
- 文件: tests/test_cte.py (新建)
- 改动:
  1. 创建测试文件 tests/test_cte.py
  2. 实现测试用例验证CTE功能（WITH ... AS ...）
  3. 验证代码：`WITH high_val AS (SELECT ID, 名称, 值 FROM Sheet WHERE 值 > 300) SELECT * FROM high_val WHERE 分类 = "B"` 应返回5行
- 预估: 10分钟
- 依赖: 无
- 风险: 测试失败可能需要修复CTE实现

### 子任务 5: 更新REQ-056状态
- 文件: REQUIREMENTS.md
- 改动: 根据子任务4的测试结果，将REQ-056的status改为"DONE"或保持"OPEN"
- 预估: 2分钟
- 依赖: 子任务4

### 子任务 6: 验证REQ-057窗口函数功能
- 文件: tests/test_window_functions.py (已存在，需补充)
- 改动:
  1. 补充窗口函数测试用例
  2. 验证 ROW_NUMBER() OVER (ORDER BY ...)
  3. 验证 RANK() OVER (PARTITION BY ... ORDER BY ...)
  4. 验证 SUM(值) OVER (PARTITION BY ...)
- 预估: 15分钟
- 依赖: 无
- 风险: 测试失败可能需要修复窗口函数实现

### 子任务 7: 更新REQ-057状态
- 文件: REQUIREMENTS.md
- 改动: 根据子任务6的测试结果，将REQ-057的status改为"DONE"或保持"OPEN"
- 预估: 2分钟
- 依赖: 子任务6

### 子任务 8: 分析REQ-060子查询IN问题根因
- 文件: src/excel_mcp_server_fastmcp/api/advanced_sql_query.py
- 改动:
  1. 分析_in_to_pandas方法（3043-3071行）
  2. 分析_current_worksheets上下文初始化（2241行）
  3. 检查子查询结果是否包含表头行
  4. 确定pandas query字符串生成问题
- 预估: 15分钟
- 依赖: 无

### 子任务 9: 修复REQ-060子查询IN实现
- 文件: src/excel_mcp_server_fastmcp/api/advanced_sql_query.py
- 改动:
  1. 修复_in_to_pandas方法中的子查询处理逻辑
  2. 确保子查询结果排除表头行
  3. 确保_current_worksheets正确初始化
  4. 验证pandas query字符串正确生成
- 预估: 20分钟
- 依赖: 子任务8
- 风险: 修复可能影响其他子查询功能

### 子任务 10: 为REQ-060编写测试
- 文件: tests/test_subquery_in.py (新建)
- 改动:
  1. 创建测试文件 tests/test_subquery_in.py
  2. 实现测试用例验证子查询IN功能
  3. 验证代码：`SELECT ID, 名称 FROM Sheet WHERE 分类 IN (SELECT DISTINCT 分类 FROM Sheet WHERE 值 > 500)` 应返回9行（B类数据）
- 预估: 10分钟
- 依赖: 子任务9

### 子任务 11: 更新REQ-060状态
- 文件: REQUIREMENTS.md
- 改动: 将REQ-060的status从"OPEN"改为"DONE"，添加完成说明
- 预估: 3分钟
- 依赖: 子任务10

### 子任务 12: 运行回归测试
- 文件: N/A (运行pytest)
- 改动:
  1. 运行 pytest tests/ -k "cte or window or subquery" 验证新测试
  2. 运行 pytest tests/test_sql_operations_consolidated.py 确保不破坏现有功能
  3. 运行 pytest tests/test_edge_cases.py 确保边界情况正常
- 预估: 15分钟
- 依赖: 子任务4, 6, 10

---

## 改动量评估

| 任务类型 | 文件数量 | 预估工时 |
|---------|---------|---------|
| 状态更新 (子任务1-3, 5, 7, 11) | 1个文件 | 16分钟 |
| 新增测试 (子任务4, 6, 10) | 2-3个文件 | 35分钟 |
| 代码修复 (子任务8, 9) | 1个文件 | 35分钟 |
| 回归测试 (子任务12) | 0个文件 | 15分钟 |
| **总计** | **4个文件** | **101分钟 (约1.7小时)** |

改动量等级：**小** (≤3个文件实际改动)

---

## 风险点

1. **REQ-056 CTE验证风险**：如果测试失败，可能需要修复CTE实现（2156-2174行）
2. **REQ-057窗口函数验证风险**：如果测试失败，可能需要修复窗口函数实现（1915-2137行）
3. **REQ-060修复风险**：修复_in_to_pandas可能影响其他子查询功能（EXISTS、NOT IN等）
4. **回归测试风险**：修复REQ-060可能影响现有子查询测试用例

---

## 技术要点

### REQ-056 CTE实现位置
- 文件：src/excel_mcp_server_fastmcp/api/advanced_sql_query.py
- 函数：_execute_query (2156-2174行)
- 关键逻辑：解析WITH子句，逐个执行CTE，将结果存储到cte_data字典

### REQ-057窗口函数实现位置
- 文件：src/excel_mcp_server_fastmcp/api/advanced_sql_query.py
- 函数：
  - _has_window_function (1911-1913行)
  - _apply_window_functions (1915-1959行)
  - _compute_window_function (1961-2010行)
- 支持函数：ROW_NUMBER, RANK, DENSE_RANK

### REQ-060子查询IN问题位置
- 文件：src/excel_mcp_server_fastmcp/api/advanced_sql_query.py
- 函数：_in_to_pandas (3043-3071行)
- 问题疑似点：
  1. 第3058行：`self._current_worksheets` 上下文未正确初始化
  2. 第3060行：`sub_result.iloc[:, 0]` 可能包含表头行
  3. 第3062行：pandas query字符串生成可能有误

---

## 依赖关系图

```
子任务1, 2, 3 (状态更新) → 无依赖
子任务4 (CTE验证) → 无依赖 → 子任务5 (更新状态)
子任务6 (窗口函数验证) → 无依赖 → 子任务7 (更新状态)
子任务8 (分析REQ-060) → 无依赖
子任务9 (修复REQ-060) → 依赖子任务8 → 子任务10 (编写测试) → 子任务11 (更新状态)
子任务12 (回归测试) → 依赖子任务4, 6, 10
```

---

## 后续建议

1. **自动化测试**：建议为所有已完成功能添加自动化测试
2. **文档更新**：建议更新用户文档说明已支持的CTE和窗口函数功能
3. **性能优化**：如果CTE或窗口函数性能不佳，可考虑优化
