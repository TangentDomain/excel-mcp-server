# ExcelMCP 任务拆解清单

## REQ-066: 数据验证写入失败（67%失败率）

### 子任务 1: REQ-066-1 分析数据验证失败原因
- 文件: src/excel_mcp_server_fastmcp/server.py (excel_set_data_validation函数, 3749-3986行)
- 改动: 添加详细日志记录，追踪每种验证类型(list/whole_number/decimal/date/text_length/custom)的执行路径，记录参数解析、DataValidation对象创建、保存过程的关键节点
- 预估: 5分钟
- 依赖: 无

### 子任务 2: REQ-066-2 编写数据验证单元测试
- 文件: tests/ 新建 test_data_validation.py
- 改动: 为6种验证类型(list, whole_number, decimal, date, text_length, custom)创建单元测试，覆盖正常和边界情况（空值、超限值、特殊字符）
- 预估: 10分钟
- 依赖: 子任务1

### 子任务 3: REQ-066-3 修复验证规则创建逻辑
- 文件: src/excel_mcp_server_fastmcp/server.py (3836-3923行)
- 改动: 根据子任务1和2的发现，修复验证条件解析、值类型转换、openpyxl DataValidation对象创建中的bug
- 预估: 15分钟
- 依赖: 子任务2

### 子任务 4: REQ-066-4 验证修复效果
- 文件: tests/test_data_validation.py
- 改动: 运行完整测试套件，确保67%失败率降至可接受范围(5%以下)
- 预估: 5分钟
- 依赖: 子任务3

---

## REQ-067: 插入列后实际未插入但报成功

### 子任务 1: REQ-067-1 分析insert_columns保存逻辑
- 文件: src/excel_mcp_server_fastmcp/core/excel_writer.py (247-320行)
- 改动: 检查_safe_save_workbook调用(workbook.close()前的顺序)、验证重新加载逻辑、确认文件句柄正确关闭
- 预估: 5分钟
- 依赖: 无

### 子任务 2: REQ-067-2 检查流式写入路径
- 文件: src/excel_mcp_server_fastmcp/api/excel_operations.py (964-987行)
- 改动: 验证StreamingWriter.update_range是否正确实现insert操作，检查返回值处理逻辑
- 预估: 5分钟
- 依赖: 无

### 子任务 3: REQ-067-3 修复保存和验证逻辑
- 文件: src/excel_mcp_server_fastmcp/core/excel_writer.py (247-320行)
- 改动: 确保在workbook.close()之前完成所有保存操作，优化验证逻辑减少文件重新加载次数
- 预估: 8分钟
- 依赖: 子任务1

### 子任务 4: REQ-067-4 测试insert_columns功能
- 文件: tests/test_core.py (test_writer_insert_columns函数, 186行)
- 改动: 扩展测试用例，验证插入后立即读取确认列数增加，检查不同位置插入(开头/中间/末尾)
- 预估: 5分钟
- 依赖: 子任务3

---

## REQ-068: 公式计算报'不支持的文件格式'

### 子任务 1: REQ-068-1 分析文件格式检测逻辑
- 文件: src/excel_mcp_server_fastmcp/core/excel_writer.py (_detect_file_format函数, 860-881行)
- 改动: 检查支持的文件格式列表(xlsx/xls/xlsm/xltx/xltm/xlsb)，验证扩展名解析是否正确
- 预估: 5分钟
- 依赖: 无

### 子任务 2: REQ-068-2 检查openpyxl load_workbook调用
- 文件: src/excel_mcp_server_fastmcp/core/excel_writer.py (set_formula: 631行, evaluate_formula: 804-811行)
- 改动: 验证load_workbook参数是否包含正确的文件格式选项，检查是否有format参数传递错误
- 预估: 5分钟
- 依赖: 子任务1

### 子任务 3: REQ-068-3 修复文件格式处理
- 文件: src/excel_mcp_server_fastmcp/core/excel_writer.py
- 改动: 根据_detect_file_format返回的格式，正确设置openpyxl的加载选项，或者添加格式兼容层
- 预估: 10分钟
- 依赖: 子任务2

### 子任务 4: REQ-068-4 测试多种格式文件
- 文件: tests/ 新建 test_formula_formats.py
- 改动: 创建测试文件(每种格式至少一个)，验证set_formula和evaluate_formula在不同格式下的表现
- 预估: 8分钟
- 依赖: 子任务3

---

## REQ-069: 写入覆盖功能异常（OperationResult无get属性）

### 子任务 1: REQ-069-1 定位.get()错误位置
- 文件: 全范围搜索 src/
- 改动: 搜索所有对OperationResult对象使用.get()方法的代码，记录位置（如server.py:4243, 4281行对meta字典的.get调用是正确的，但需要确认data字段中的result对象是否被错误访问）
- 预估: 5分钟
- 依赖: 无

### 子任务 2: REQ-069-2 修复错误的属性访问
- 文件: src/excel_mcp_server_fastmcp/server.py (excel_write_only_override函数, 4176-4309行)
- 改动: 将对OperationResult对象的.get()调用改为属性访问(如result.success改为直接访问，result.data改为result.data等)
- 预估: 5分钟
- 依赖: 子任务1

### 子任务 3: REQ-069-3 检查其他类似问题
- 文件: 全范围搜索 src/excel_mcp_server_fastmcp/
- 改动: 搜索所有使用format_operation_result或_wrap包装OperationResult的地方，确认是否正确处理了dataclass和dict的区别
- 预估: 10分钟
- 依赖: 子任务2

### 子任务 4: REQ-069-4 测试写入覆盖功能
- 文件: tests/ 新建 test_write_override.py
- 改动: 创建测试用例，验证write_only_override在不同数据规模下(小/中/大)的行为，确认返回值格式正确
- 预估: 5分钟
- 依赖: 子任务3

---

## REQ-070: 双行表头识别不一致（describe_table vs get_headers）

### 子任务 1: REQ-070-1 对比两种识别算法
- 文件: src/excel_mcp_server_fastmcp/server.py (_detect_dual_header: 2507-2538行) 和 src/excel_mcp_server_fastmcp/api/excel_operations.py (_parse_dual_header_data: 532-615行)
- 改动: 详细记录两种算法的判断逻辑差异（server.py检查第2行是否全英文字母且第1行有中文；excel_operations.py直接读取两行不检测），输出对比文档
- 预估: 8分钟
- 依赖: 无

### 子任务 2: REQ-070-2 统一双行表头识别逻辑
- 文件: src/excel_mcp_server_fastmcp/api/excel_operations.py (303-391行, 455-615行)
- 改动: 将_detect_dual_header的检测逻辑整合到_build_header_range或_parse_dual_header_data中，确保get_headers使用与describe_table相同的判断标准
- 预估: 15分钟
- 依赖: 子任务1

### 子任务 3: REQ-070-3 更新describe_table调用
- 文件: src/excel_mcp_server_fastmcp/server.py (2680-2804行)
- 改动: 让describe_table也调用统一的get_headers接口获取表头信息，而不是重复实现检测逻辑
- 预估: 10分钟
- 依赖: 子任务2

### 子任务 4: REQ-070-4 测试双行表头场景
- 文件: tests/ 新建 test_dual_header_consistency.py
- 改动: 创建测试用例：单行表头、标准双行表头(中文+英文)、不完整双行表头、合并单元格场景，验证两个API返回一致
- 预估: 10分钟
- 依赖: 子任务3

---

## REQ-071: 修复Conventional Commits提交格式违规

### 子任务 1: REQ-071-1 修正提交4d230c9
- 文件: Git仓库 (提交4d230c9)
- 改动: 执行git commit --amend，修改提交信息从"[REQ-065] DONE + 新增REQ-066~070"改为"[REQ-065] type: DONE + 新增REQ-066~070"（或使用合适的type前缀如fix:/feat:）
- 预估: 2分钟
- 依赖: 无

### 子任务 2: REQ-071-2 验证提交格式
- 文件: Git日志
- 改动: 执行git log -1验证提交信息格式符合CONVENTIONAL_COMMITS.md规范
- 预估: 1分钟
- 依赖: 子任务1

---

## 统计信息

- **总子任务数**: 24
- **涉及文件数**: 12个主要文件
- **预估总耗时**: 约150分钟（2.5小时）
- **改动量评估**:
  - 小改动（≤3文件）: REQ-071 (2个子任务)
  - 中等改动（4-8文件）: REQ-067, REQ-068, REQ-069 (12个子任务)
  - 大改动（≥9文件）: REQ-066, REQ-070 (10个子任务)

## 风险点

1. **REQ-066**: 67%失败率可能涉及openpyxl底层限制，需要深入调试每种验证类型
2. **REQ-067**: 插入列后验证逻辑可能与实际保存状态不同步，需要确保原子性
3. **REQ-068**: 不同文件格式(.xls vs .xlsx)可能需要不同的处理逻辑
4. **REQ-069**: OperationResult是dataclass，与dict混用可能引发多处错误
5. **REQ-070**: 两个API的表头识别算法差异较大，统一可能需要重构
