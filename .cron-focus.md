## 当前优先级

1. **性能优化（P0）**：用python-calamine替换openpyxl读路径
   - 验收：get_range < 50ms（当前1.6s）
   - 不破坏现有API，761测试全通过

2. **工具描述优化（P1）**：REQ-006，每轮MCP验证自然跟进

3. **工程治理（P1）**：REQ-010，持续迭代

4. **可观测性（P2）**：REQ-013，结构化日志

## 临时指令
（本轮无临时指令）
